before:
  hooks:
    - go mod tidy
builds:
  - binary: coolify
    goos:
      - darwin
      - linux
      - windows
    goarch:
      - amd64
      - arm64
    env:
      - CGO_ENABLED=0
    # Build all platforms in parallel using all available CPU cores
    parallelism: -1