overlay: 1.0.0
info:
  title: Remove UUID Format Overlay
  version: 1.0.0

actions:
  - target: "$..[?(@.format == 'uuid')]"
    description: "Remove all format: uuid fields"
    update:
      format: null
